<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }

        table,
        th,
        td {
            border: 1px solid black;
        }

        th,
        td {
            padding: 15px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        form {
            display: flex;
            flex-direction: column;
            width: 50%;
            margin: 0 auto;
        }

        input {
            margin-bottom: 10px;
            height: 30px;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 100%;
        }

        label {
            font-size: 20px;

        }
    </style>
</head>

<body>
    <form action="/users/<%user.id%>?method=PATCH" method="post">
        <!-- The form action is set to /users, which is the route that handles the POST request to update the user's username. -->
        <label for="">This Email name: <b>
                <%= user.email %>
            </b> </label>
        <br> <br>
        <br> <br>
        <!-- The form action is set to /users, which is the route that handles the POST request to update the user's username. -->
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" value="<%= user.username %>">
        <input type="password" name="password" id="" value="<%= user.password %>">
        <input type="hidden" name="id" value="<%= user.id %>">
        <button type="submit">Submit</button>
    </form>
</body>

</html>